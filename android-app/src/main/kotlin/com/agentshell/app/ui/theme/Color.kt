package com.agentshell.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────

/** Deep electric blue — primary action color */
val BluePrimary = Color(0xFF3B82F6)
val BluePrimaryLight = Color(0xFF60A5FA)
val BluePrimaryDark = Color(0xFF2563EB)

/** Vibrant green — success, local providers */
val GreenSuccess = Color(0xFF22C55E)
val GreenSuccessLight = Color(0xFF4ADE80)
val GreenSuccessDark = Color(0xFF16A34A)

/** Purple — tertiary accent */
val PurpleAccent = Color(0xFF8B5CF6)
val PurpleAccentLight = Color(0xFFA78BFA)
val PurpleAccentDark = Color(0xFF7C3AED)

/** Amber — warnings, pending approvals */
val AmberWarning = Color(0xFFF59E0B)
val AmberWarningLight = Color(0xFFFBBF24)
val AmberWarningDark = Color(0xFFD97706)

/** Red — errors, failed states */
val RedError = Color(0xFFEF4444)
val RedErrorLight = Color(0xFFF87171)
val RedErrorDark = Color(0xFFDC2626)

// ── Surface palette (Dark) ───────────────────────────────────────────────────

val DarkBackground = Color(0xFF0F172A)        // Slate 900
val DarkSurface = Color(0xFF1E293B)           // Slate 800
val DarkSurfaceVariant = Color(0xFF334155)    // Slate 700
val DarkOnSurface = Color(0xFFF1F5F9)         // Slate 100
val DarkOnSurfaceVariant = Color(0xFF94A3B8)  // Slate 400
val DarkOutline = Color(0xFF475569)           // Slate 600

// ── Surface palette (Light) ──────────────────────────────────────────────────

val LightBackground = Color(0xFFF8FAFC)       // Slate 50
val LightSurface = Color(0xFFFFFFFF)           // White
val LightSurfaceVariant = Color(0xFFF1F5F9)   // Slate 100
val LightOnSurface = Color(0xFF0F172A)        // Slate 900
val LightOnSurfaceVariant = Color(0xFF64748B) // Slate 500
val LightOutline = Color(0xFFCBD5E1)          // Slate 300

// ── Terminal colors (Catppuccin Mocha) ───────────────────────────────────────

val TerminalBg = Color(0xFF1E1E2E)
val TerminalFg = Color(0xFFCDD6F4)
val TerminalGreen = Color(0xFFA6E3A1)
val TerminalRed = Color(0xFFF38BA8)
val TerminalYellow = Color(0xFFF9E2AF)
val TerminalBlue = Color(0xFF89B4FA)
val TerminalComment = Color(0xFF6C7086)
val TerminalInput = Color(0xFF313244)
